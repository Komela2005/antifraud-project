version: "3.9"

services:

  antifraud-app:
    build: .
    container_name: antifraud-streamlit
    ports:
      - "8501:8501"
    volumes:
      - ./database:/app/database
    restart: unless-stopped
