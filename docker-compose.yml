version: "3.9"

services:

  antifraud-app:
    build: .
    container_name: antifraud-streamlit
    ports:
      - "8501:8501"
    volumes:
      - sqlite_data:/app/database
    restart: unless-stopped

volumes:
  sqlite_data:
